void insertion(student *node)
{
	student node start, *previous, *new1;
	nw = start.next;
	previous = &start;
	new1 = new student;
	new1->next = next ;
	previous->next = new1;
	cout<<"\n Input the first student details: ");
	cin>>new1->insert();

}
